# Images & Files (Anthropic)

- **Images:** Mix text and images in message `content` as blocks per the Messages schema.
- **Files API (beta):** Upload/manage files once and reuse them across calls; requires `anthropic-beta: files-api-2025-04-14`.
