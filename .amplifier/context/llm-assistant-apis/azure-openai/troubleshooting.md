# Troubleshooting (Azure OpenAI)

- **PDF input “purpose”:** Use `assistants` temporarily (not `user_data`) until the known issue is resolved.
- **Unsupported features:** Web search tool is not yet supported on Azure OpenAI Responses.
- **Streaming performance with background mode:** Known perf note—see the page for current guidance.
